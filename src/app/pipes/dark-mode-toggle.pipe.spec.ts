import { DarkModeTogglePipe } from './dark-mode-toggle.pipe';

describe('DarkModeTogglePipe', () => {
  it('create an instance', () => {
    const pipe = new DarkModeTogglePipe();
    expect(pipe).toBeTruthy();
  });
});
